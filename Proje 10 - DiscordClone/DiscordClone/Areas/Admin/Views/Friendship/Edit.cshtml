@model DiscordClone.Models.Friendship

@{
    ViewData["Title"] = "İlişki Düzenle";
}

<div class="card bg-dark text-white border-secondary p-4" style="max-width: 600px;">
    <h4 class="fw-bold text-white mb-4"><i class="bi bi-pencil-square text-warning me-2"></i>İlişki Durumu Düzenle</h4>

    <p class="small text-muted-discord">
        <strong>Gönderen:</strong> @Model.Sender?.UserName <br>
        <strong>Alıcı:</strong> @Model.Receiver?.UserName
    </p>

    <form asp-action="Edit" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="SenderId" />
        <input type="hidden" asp-for="ReceiverId" />

        <div class="mb-4">
            <label asp-for="Status" class="form-label">Durum</label>
            <select asp-for="Status" class="form-select" required>
                <option value="@FriendshipStatus.Pending">Beklemede (Pending)</option>
                <option value="@FriendshipStatus.Accepted">Kabul Edildi (Accepted)</option>
                <option value="@FriendshipStatus.Blocked">Engellendi (Blocked)</option>
            </select>
            <span asp-validation-for="Status" class="text-danger"></span>
        </div>

        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-warning text-dark fw-bold px-4">Kaydet</button>
            <a asp-action="Index" class="btn btn-outline-light px-4">İptal</a>
        </div>
    </form>
</div>
